name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - Name: Print greeting
        run: echo "hello Soeren"
      - Name: Print goodbye
        run: echo "Done"
